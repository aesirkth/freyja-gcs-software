[project]
name = "dashboard"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
  "fastapi[standard]",
  "pydantic",
  "pyserial",
  "protobuf>=5",
  "uvicorn",
  "numpy",
  "pymap3d",
  "websockets",
]
